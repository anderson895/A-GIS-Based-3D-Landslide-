# Python dependencies for the landslide hazard pipeline.
# Install into the project virtual environment:
#     .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# rasterio ships its own bundled GDAL on Windows wheels, so no separate
# GDAL/QGIS installation is required to run the pipeline.

numpy>=1.24
scipy>=1.10
rasterio>=1.3
pillow>=9.5
requests>=2.31
pyyaml>=6.0
matplotlib>=3.7
